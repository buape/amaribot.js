class ClientOptions {
    /**
     *
     * @constructs ClientOptions
     * @description Options for the client when initalizing
     */
    constructor(data = {}) {
    }
}

module.exports = ClientOptions